# digitize-it-mock

This project has been moved to:

https://gitlab.opencode.de/digitalservicebund/hackathon-automatisierte-einkommenspruefung/digitize-it-mock

## Update your Git remote

If you have a local clone of this repository, update the remote URL to point to the new location.

**Using SSH:**
```bash
git remote set-url origin git@gitlab.opencode.de:digitalservicebund/hackathon-automatisierte-einkommenspruefung/digitize-it-mock.git
```

**Using HTTPS:**
```bash
git remote set-url origin https://gitlab.opencode.de/digitalservicebund/hackathon-automatisierte-einkommenspruefung/digitize-it-mock.git
```

Verify with:
```bash
git remote -v
```
